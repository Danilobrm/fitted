import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  errors: {
    width: '80%',
    display: 'flex',
    alignSelf: 'center',
    textAlign: 'left',
    position: 'relative',
    top: -10,
  },
  errorsText: {
    width: '100%',
    color: 'red',
  },
});

export default style;
